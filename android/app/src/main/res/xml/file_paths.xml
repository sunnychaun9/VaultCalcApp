<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Expose the temporary share directory inside the vault dir.
         getVaultDirectory() returns {filesDir}/vault, so share_temp
         lives at {filesDir}/vault/share_temp/. -->
    <files-path name="share_temp" path="vault/share_temp/" />
</paths>
